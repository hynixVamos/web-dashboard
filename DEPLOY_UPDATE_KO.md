# 신고가 기능 수정본 — 직접 배포 안내

이 수정본은 GitHub `hynixVamos/web-dashboard`의 `63e1661`을 기준으로 만들었습니다.
바탕화면의 기존 폴더는 신고가 기능이 없는 이전 버전이라 최신 저장소 코드를 사용했습니다.
이후 사용자 요청으로 GitHub 업로드 및 Render 배포를 진행했습니다.

## 변경 내용

- `/us-new-highs`: 미국 상장 주식·ADR의 52주 신고가, 업종 선택/검색/정렬.
- 미국은 **당일 정규장 고가 > 직전 364일 최고 고가**입니다. 같은 가격은 제외합니다.
  `종가 돌파만 보기`를 선택하면 종가도 이전 52주 고가를 넘어야 합니다.
- 바이오·반도체·소프트웨어 등 한국어 업종을 사용합니다. 네이버 업종을 사용하지 않습니다.
- 국내는 20일/60일/52주/역사적 모두 **당일 종가 > 이전 기간 최고 고가**로 변경했습니다.
  고점 돌파 후 종가가 이전 고가 이하이면 제외됩니다. 단순히 윗꼬리가 있다는 이유만으로 제외하지는 않습니다.
- 국내 스팩·리츠를 일봉 수집 전에 제외합니다. 메리츠금융지주는 이름 때문에 제외되지 않습니다.
- 기존 국내 종목 메모·수동 섹터와 나머지 대시보드 기능을 유지합니다.
- 네이버 시가총액 순위의 페이지 경계에서 종목이 중복되더라도 전체 수집을 중단하지 않습니다.
  중복은 제거하고, 실제로 누락된 주식은 개별 수집 실패로 표시합니다.
- 네이버가 시간외 거래 등으로 시세를 아직 OPEN 상태로 제공하면 확정 시세 대기로 표시하고
  5분마다 다시 확인합니다. 미확정 시세를 종가로 간주하지 않습니다.

## 파일 적용

1. GitHub 저장소의 현재 내용을 백업합니다.
2. `web-dashboard-updated.zip`을 풀고 안의 `web-dashboard` 폴더 **내용물**을
   저장소 루트(`app.py`, `Procfile`이 있는 위치)에 적용합니다.
   또는 `web-dashboard-changed-files.zip`의 파일만 같은 상대 경로에 덮어씁니다.
3. 특히 `templates/`, `static/` 폴더 내부 파일과 새 Python 파일, `requirements.txt`를 함께 올립니다.
   저장소에 남아 있는 `web-dashboard-sector-notes-ready`라는 과거 복사 폴더에 넣지 마세요.
4. 직접 커밋·푸시한 뒤 Render에서 배포합니다. 자동 배포가 켜져 있다면 푸시로 시작될 수 있습니다.
5. 빌드 명령은 `pip install -r requirements.txt`, 시작 명령은 기존의
   `gunicorn -c gunicorn.conf.py app:app`을 그대로 사용합니다. Python 3.12 이상을 권장합니다.
6. 배포 후 `/new-highs`, `/us-new-highs`를 확인합니다.

## 수집과 저장

- 미국은 Nasdaq Trader 종목 목록과 Yahoo Finance 일봉·업종을 사용합니다. 별도 API 키는 필요 없습니다.
- ETF·테스트 종목·우선주·워런트·권리·유닛·채권 등은 목록에서 제외합니다.
  전체 미국 증권 또는 OTC 전체를 보장하는 데이터는 아닙니다.
- 뉴욕 시간 16:30 이후 당일 마감 데이터를 사용합니다. 그 전에는 직전 마감 거래일을 사용합니다.
  조기 폐장일에도 16:30 이후 수집합니다. 휴일은 SPY의 실제 거래일로 확인합니다.
- 미국은 250개씩 처리하고 저장합니다. 최초 수집은 수천 종목을 조회하므로 오래 걸릴 수 있습니다.
  화면에 처리 수/대기/실패와 불완전한 목록 여부가 표시됩니다. 외부 제공처 제한 시 실패 종목은 재시도합니다.
- 국내의 이전 고가 기준 보고서는 새 종가 기준과 혼동되지 않도록 표시를 중단합니다.
  최신 거래일은 자동 재계산합니다. 과거 날짜 파일은 자동 소급 재계산하지 않으며 안내가 표시됩니다.
- 기존 `NEW_HIGHS_DATA_DIR`와 `NEW_HIGHS_NOTES_DB` 설정 및 저장 파일을 유지하세요.
  미국 기본 저장 위치는 국내 데이터 폴더의 형제 폴더 `us_new_highs`입니다.
  Render 영구 디스크를 쓰는 경우 `US_NEW_HIGHS_DATA_DIR`도 해당 디스크 안으로 지정하세요
  (예: `/var/data/us_new_highs`). 영구 디스크가 없으면 재배포 시 수집 이력이 사라질 수 있습니다.

## 미국 섹터 직접 조정

`us_sector_overrides.json`에서 `"티커": "분류"`를 추가/수정합니다.
예: `"NVDA": "반도체"`, `"NBIS": "AI 인프라"`.
해당 파일에 없는 종목은 `us_sectors.py`의 업종 변환 규칙으로 분류합니다.
미분류를 임의의 업종으로 추정하지 않습니다.

파일의 직접 지정 분류는 기존 미국 보고서에도 조회 시 적용됩니다.
수동 수집은 `python us_new_highs.py --batch-size 250`이며 여러 번 실행하면 이어서 진행합니다.
`--force`는 해당 거래일 체크포인트를 초기화하므로 처음 한 번만 사용합니다.

## 검증

`python -m unittest discover -s tests -v`

`node --test tests/new-highs-model.test.cjs`

가격 경계값, 윗꼬리, 신규/연속 초기화, 스팩·리츠 제외, 수집 재개와 실패 시 보존,
API의 외부 통신 금지, 기존 메모 동작을 검증합니다.
